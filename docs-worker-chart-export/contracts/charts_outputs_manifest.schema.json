{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.local/schemas/charts_outputs_manifest.schema.json",
  "title": "ChartsOutputsManifest",
  "description": "Выходной артефакт шага CHART_EXPORT. Записывается воркером в GCS и содержит и параметры запроса (requested/minImages), и фактически полученные результаты (items/failures).",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "runId", "stepId", "createdAt", "symbol", "timeframe", "minImages", "requested", "items"],
  "properties": {
    "schemaVersion": { "type": "integer", "minimum": 1 },
    "runId": {
      "type": "string",
      "description": "Flow run docId.",
      "pattern": "^[0-9]{8}-[0-9]{6}_[A-Z0-9]+(?:-[A-Z0-9]+)*_[a-z0-9]{3,6}$"
    },
    "stepId": { "type": "string", "minLength": 1 },
    "createdAt": { "type": "string", "format": "date-time" },
    "timeframe": { "type": "string", "minLength": 1 },
    "symbol": { "type": "string", "minLength": 1 },
    "minImages": {
      "type": "integer",
      "minimum": 0,
      "description": "Порог успеха шага. Обычно это копия из `flow_run.steps[stepId].inputs.minImages`."
    },
    "requested": {
      "type": "array",
      "minItems": 1,
      "description": "Что просили сгенерировать. Обычно это копия из `flow_run.steps[stepId].inputs.requests`.",
      "items": { "$ref": "#/$defs/chartRequest" }
    },
    "items": {
      "type": "array",
      "minItems": 0,
      "description": "Что реально удалось получить (ответ/результат генерации).",
      "items": { "$ref": "#/$defs/chartItem" }
    },
    "failures": {
      "type": "array",
      "description": "Какие элементы не удалось получить (для диагностики нестабильного внешнего API). Это результат выполнения.",
      "items": { "$ref": "#/$defs/chartFailure" }
    }
  },
  "$defs": {
    "chartRequest": {
      "type": "object",
      "additionalProperties": false,
      "required": ["chartTemplateId"],
      "properties": {
        "chartTemplateId": { "type": "string", "minLength": 1 }
      }
    },
    "chartItem": {
      "type": "object",
      "additionalProperties": false,
      "required": ["chartTemplateId", "kind", "generatedAt", "png_gcs_uri"],
      "properties": {
        "chartTemplateId": {
          "type": "string",
          "minLength": 1,
          "description": "Шаблон, по которому построено именно это изображение."
        },
        "kind": {
          "type": "string",
          "description": "Тип/вид картинки. Для прототипа — свободная строка (например: price, volume, rsi, macd). В MVP для CHART_EXPORT заполняется из шаблона (поле description у соответствующего chartTemplateId).",
          "minLength": 1
        },
        "generatedAt": {
          "type": "string",
          "format": "date-time",
          "description": "Время генерации конкретного изображения (UTC, RFC3339)."
        },
        "label": { "type": "string" },
        "png_gcs_uri": { "type": "string", "pattern": "^gcs://.+" },
        "signed_url": { "type": "string" },
        "expires_at": { "type": "string", "format": "date-time" },
        "meta": {
          "type": "object",
          "description": "Доп. параметры рендера/индикаторов (без строгой схемы на старте).",
          "additionalProperties": true
        }
      }
    },
    "chartFailure": {
      "type": "object",
      "additionalProperties": false,
      "required": ["request", "error"],
      "properties": {
        "request": { "$ref": "#/$defs/chartRequest" },
        "error": {
          "type": "object",
          "additionalProperties": false,
          "required": ["code", "message"],
          "properties": {
            "code": { "type": "string", "minLength": 1 },
            "message": { "type": "string", "minLength": 1 },
            "details": {
              "type": "object",
              "additionalProperties": true
            }
          }
        }
      }
    }
  }
}


